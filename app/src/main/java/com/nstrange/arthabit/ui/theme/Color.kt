package com.nstrange.arthabit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary ──
val Primary = Color(0xFF007AFF)        // iOS Blue
val Secondary = Color(0xFF5856D6)      // iOS Purple

// ── Light theme colors ──
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFF2F2F7)
val LightOnBackground = Color(0xFF000000)
val LightOnSurface = Color(0xFF000000)

// ── Dark theme colors (used for Profile, Expense items) ──
val DarkBackground = Color(0xFF000000)
val DarkSurface = Color(0xFF1C1C1E)
val DarkSurfaceVariant = Color(0xFF2C2C2E)
val DarkOnBackground = Color(0xFFFFFFFF)
val DarkOnSurface = Color(0xFFFFFFFF)
val DarkTextSecondary = Color(0xFF8E8E93)

// ── Status Colors ──
val StatusSuccess = Color(0xFF34C759)
val StatusWarning = Color(0xFFFF9500)
val StatusError = Color(0xFFFF3B30)

// ── Neo-brutalist card colors ──
val CardBorder = Color(0xFF000000)
val CardShadow = Color(0xFF000000)
val CardBackground = Color(0xFFFFFFFF)

// ── Currency toggle ──
val CurrencySelected = Color(0xFF007AFF)
val CurrencyUnselected = Color(0xFFE5E5EA)

